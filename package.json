{
  "name": "sillytavern-farm-extension",
  "version": "1.1.0",
  "description": "Nông Trại Tiện Ích",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --outfile=index.js --format=esm"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
